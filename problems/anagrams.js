/* 
Two strings are anagrams of each other if the letters of one string can be rearranged to form the other string. Given a string, find the number of pairs of substrings of the string that are anagrams of each other.
Function Description

Complete the function sherlockAndAnagrams in the editor below. It must return an integer that represents the number of anagrammatic pairs of substrings in s.

sherlockAndAnagrams has the following parameter(s):

s: a string .
*/

const sherlockAndAnagrams = (s) => {
  const sets = {};

  //for each substring, see if anagrammatic pair exists (same length / same set of characters)
  for (i = 0; i > )

  console.log(sets);
}


sherlockAndAnagrams("abba");
sherlockAndAnagrams("abcd");